## v0.6.1 - 2026-06-22
### Added
* Implemented the `locked` attribute on dokku_app, which manages an app deploy lock via apps:lock/apps:unlock and reads its state via apps:locked
### Fixed
* Surface errors that were previously ignored during app updates (config unset, buildpack and nginx changes), when reading app config, and when creating a service link, so failures are reported instead of silently succeeding